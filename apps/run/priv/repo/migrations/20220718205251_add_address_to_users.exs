defmodule Run.Repo.Migrations.AddAddressToUsers do
  use Ecto.Migration

  def change do
    # alter table(:users) do
    #   add :first_name, :string
    #   add :last_name, :string
    #   add :street_and_house_nr, :string
    #   add :zip_code, :string
    #   add :city, :string
    #   add :country, :string, default: "Danmark"
    #   add :date_of_birth, :date
    # end
  end
end
